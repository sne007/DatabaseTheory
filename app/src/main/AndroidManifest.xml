<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.snehith.chall.database_theory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/baseline_view_agenda_black_48"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/baseline_view_agenda_black_48"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Details" android:screenOrientation="portrait"></activity>
    </application>

</manifest>